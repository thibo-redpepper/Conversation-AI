# GoHighLevel / LeadConnector
# NOTE: Each subaccount has its own private token + locationId

# Vastgoed
GHL_VASTGOED_PRIVATE_TOKEN=
GHL_VASTGOED_LOCATION_ID=

# Dakwerken
GHL_DAKWERKEN_PRIVATE_TOKEN=
GHL_DAKWERKEN_LOCATION_ID=

# Gevelwerken
GHL_GEVELWERKEN_PRIVATE_TOKEN=
GHL_GEVELWERKEN_LOCATION_ID=

# Optional fallback (legacy single account)
GHL_PRIVATE_TOKEN=
GHL_LOCATION_ID=
GHL_API_VERSION=2021-07-28

# OpenAI (optional)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Pricing per 1M tokens (defaults reflect current OpenAI pricing for gpt-4o-mini)
OPENAI_PRICE_INPUT_PER_1M=0.15
OPENAI_PRICE_OUTPUT_PER_1M=0.60
# FX rate for € estimate (default 0.92 if not set)
USD_TO_EUR_RATE=0.92

# Supabase (sync + draft storage)
SUPABASE_PROJECT_ID=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
HASH_SALT=
SYNC_ENABLED=false
SYNC_INTERVAL_SEC=120
SYNC_MAX_CONVERSATIONS=50
SYNC_MAX_PAGES=10
SYNC_MESSAGE_LIMIT=50
SYNC_MAX_MESSAGES=200
SYNC_CREATE_DRAFTS_ON_BACKFILL=false

# Lost drafts (test mode, does not update GHL)
LOST_DRAFTS_ENABLED=false
# Comma-separated list of account names or location IDs (e.g. Dakwerken)
LOST_DRAFTS_ACCOUNTS=
LOST_DRAFTS_ON_BACKFILL=false

# Mailgun (for CRM email sending)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
MAILGUN_BASE_URL=https://api.mailgun.net
MAILGUN_FROM=
MAILGUN_SIGNING_KEY=

# Twilio (for CRM SMS sending)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
# Use one of the two:
TWILIO_FROM_NUMBER=
TWILIO_MESSAGING_SERVICE_SID=
# Inbound webhook (public URL)
TWILIO_WEBHOOK_URL=

# CRM Auto-reply rules
# Run server/db/auto_reply.sql once to create the table.

# Supabase (optional, for AI draft logging)
SUPABASE_PROJECT_ID=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
# Redaction/hash
HASH_SALT=
# Sync inbound messages to Supabase
SYNC_ENABLED=false
SYNC_INTERVAL_SECONDS=60
